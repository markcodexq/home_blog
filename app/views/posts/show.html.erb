<div class="container">
  <%= render 'form', post: @post %>
  <%= link_to 'Back', posts_path %>
</div>

<section class="comments">
  <div class="commetn-form">
    <h3>Leave a reply</h3>
    <%= render 'comments/form' %>
  </div>
  <div class="container">
    <h2>Comments: <%= @post.comments.count %></h2>
    <%= render @post.comments %>
  </div>
</section>